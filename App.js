import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <Text>Initial</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
});

export default App;
